/**
 * Shared API base URL for UniERP frontend pages.
 * Override before this script loads, or edit the fallback below.
 * Development fallback: local Spring Boot default port.
 */
window.UNI_ERP_API_BASE_URL =
  window.UNI_ERP_API_BASE_URL || 'http://localhost:8080';
